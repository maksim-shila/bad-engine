export class GameObjectContainer {
    constructor(scene) {
        this.scene = scene;
        this.GlobalType = "container";
    }
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    update(_) { }
}
